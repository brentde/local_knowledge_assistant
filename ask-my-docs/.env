# .env.example - copy to .env and set values
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini   # or gpt-4o, or gpt-4o-mini-0613 depending on availability
EMBEDDING_MODEL=text-embedding-3-small
CHROMA_PERSIST_DIR=/data/chroma
BACKEND_PORT=8000
FRONTEND_PORT=8501
